{% extends 'base.html.twig' %}
{% block body %}
  <h1>Créer une figurine</h1>
  {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.imageName) }}
    {{ form_row(form.price) }}
    <button class="btn btn-primary">Enregistrer</button>
  {{ form_end(form) }}
{% endblock %}
